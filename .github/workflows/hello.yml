name: Hello Actions

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Saludo
        run: echo "Hola desde GitHub Actions"

      - name: Ver sistema operativo
        run: uname -a

      - name: Ver archivos
        run: ls -la